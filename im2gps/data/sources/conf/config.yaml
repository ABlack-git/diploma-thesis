defaults:
  - creds: example
  - filters: filters
  - db: local
  - checkpoint: meta

app:
  download: null # meta, photos
  checkpoint_type: null # null(start from beginning), from_db, from_config
  data_directory: "" # path to data root directory